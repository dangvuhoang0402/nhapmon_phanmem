<!DOCTYPE html>
<html>
<head>
    <title>Cập Nhật Qui Định Thành Công</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="container">
        <div class="header">
            <h1><i class="fas fa-check-circle"></i> Cập Nhật Qui Định Thành Công</h1>
            <a href="/home" class="back-btn"><i class="fas fa-arrow-left"></i> Trở về Trang Chủ</a>
        </div>

        <div class="success-content">
            <div class="rules-info">
                <h3>Qui Định Về Độc Giả</h3>
                <table class="info-table">
                    <tr>
                        <th>Tuổi Tối Thiểu:</th>
                        <td><%= rules.ReaderRules.MinAge %> tuổi</td>
                    </tr>
                    <tr>
                        <th>Thời Hạn Thẻ:</th>
                        <td><%= rules.ReaderRules.CardValidityMonths %> tháng</td>
                    </tr>
                </table>
            </div>

            <div class="action-buttons">
                <a href="/rules/reader" class="btn">
                    <i class="fas fa-edit"></i> Tiếp Tục Chỉnh Sửa
                </a>
                <button onclick="window.print()" class="print-btn">
                    <i class="fas fa-print"></i> In Qui Định
                </button>
            </div>
        </div>
    </div>

    <style>
        .success-content {
            background: white;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.1);
            margin: 2rem auto;
            max-width: 800px;
        }
        .rules-info {
            margin-bottom: 2rem;
        }
        .info-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1rem;
        }
        .info-table th, .info-table td {
            padding: 12px;
            border-bottom: 1px solid #eee;
        }
        .info-table th {
            width: 200px;
            text-align: left;
            color: #666;
        }
        .action-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            margin-top: 2rem;
        }
        .btn, .print-btn {
            padding: 10px 20px;
            border-radius: 4px;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        .btn {
            background: #007bff;
            color: white;
            border: none;
        }
        .print-btn {
            background: #28a745;
            color: white;
            border: none;
        }
        .btn:hover, .print-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 2px 8px rgba(0,0,0,0.2);
        }
    </style>
</body>
</html>